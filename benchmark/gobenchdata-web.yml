title: Go SDK - Performance tests
repository: https://github.com/firebolt-db/firebolt-go-sdk
benchmarksFile: benchmarks.json
chartGroups:
  - name: Performance tests results 🚀
    description: |
      This is the performance tests results of the Firebolt Go SDK
    charts:
      - name: SELECT * from lineitem
        benchmarks: [ 'SelectLineItem.' ]
      - name: Select 1
        benchmarks: [ 'Select1.' ]