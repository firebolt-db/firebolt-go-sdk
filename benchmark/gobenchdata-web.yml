title: Go SDK - Performance tests
repository: https://github.com/firebolt-db/firebolt-go-sdk
benchmarksFile: benchmarks.json
chartGroups:
  - name: 🚀 Performance tests results of the Firebolt Go SDK 🚀
    charts:
      - name: SELECT * from lineitem ORDER BY l_orderkey LIMIT 1000; 🛒
        benchmarks: [ 'SelectLineItem.' ]
      - name: SELECT 1; ⚡
        benchmarks: [ 'Select1.' ]